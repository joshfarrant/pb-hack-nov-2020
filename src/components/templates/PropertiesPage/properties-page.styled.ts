import styled from 'styled-components';

export const StyledPropertiesList = styled.div`
  display: grid;
  grid-gap: 35px;
  max-width: 375px;
  margin: 0 auto;
`;

export const StyledHeading = styled.h1`
  font-size: 22px;
  margin: 8px 0 20px;
`;

export const StyledContainer = styled.div`
  padding: 15px;
  box-sizing: border-box;
`;
